/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['example.com'],
    },
    pageExtensions: ['tsx'],
};


export default nextConfig;